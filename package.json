{
  "name": "vera",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "node scripts/check-env.mjs && concurrently -n backend,frontend -c cyan,green \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && uv run uvicorn app.main:app --reload",
    "frontend": "cd frontend && npm run dev",
    "doctor": "node scripts/doctor.mjs",
    "clean:secrets": "node scripts/clean-secrets.mjs",
    "clean:data": "node scripts/clean-data.mjs",
    "clean": "npm run clean:secrets && npm run clean:data"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
